# Dependencies
node_modules/

# Build output
dist/
build/
release/
# ...trừ icon đóng gói: electron-builder đọc build/ theo mặc định, và
# package.json trỏ thẳng vào tệp này.
!build/
!build/icon.ico

# Database files
data/

# Trash (files to be deleted manually)
trash/

# Environment
.env
.vite
.DS_Store

# OS files
Thumbs.db
desktop.ini

# Kết quả kiểm thử (sinh lại được)
tests/reports/

# Sản phẩm dựng video demo (nặng, sinh lại được)
demo/build/
demo/out/

# Ban thiet ke (xem cuc bo, khong can commit)
MoneyFlow-ThietKeLai.html
